# Local Go build artifacts (the module builds to ./printcap on non-Windows).
# The committed Windows build printcap.exe is intentionally tracked.
/printcap
*.test
*.out
coverage.txt

# Runtime log files (never version-controlled).
printcap.log
*.log
